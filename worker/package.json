{
  "name": "devmorphix-ops-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that proxies Google Sheets task data as read-only JSON for the dashboard",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
